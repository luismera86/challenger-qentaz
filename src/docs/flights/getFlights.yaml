paths:
  /api/flight:
    get:
      tags:
        - Flights
      summary: 'List all flights'
      description: This endpoint is used to list all the flights registered in the database.

      responses:
        '200':
          description: Returns the list of flights that are in the database 
