paths:
  /api/airport:
    get:
      tags:
        - Airports
      summary: 'List all airports'
      description: This endpoint is used to list all the airports registered in the database.

      responses:
        '200':
          description: Returns the list of airports that are in the database
