paths:
  /api/airline:
    get:
      tags:
        - Airline
      summary: 'List all airlines'
      description: This endpoint is used to list all the airlines registered in the database.

      responses:
        '200':
          description: Returns the list of airlines that are in the database
